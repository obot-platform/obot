---
name: obot-scan
description: Scan local AI client configuration and submit it to Obot.
disable-model-invocation: true
---

# Run Obot Scan

Run the Obot CLI scan command:

```sh
obot scan
```

Report the CLI result to the user and ask if they would like to submit.
If they answer yes, then run the following command:

```sh
obot scan --non-interactive --submit
```
