---
name: obot
description: Use the Obot CLI to discover skills and MCP servers. Load this skill before running the Obot CLI.
---

# Obot

Use the local `obot` CLI for Obot skill discovery, skill installation, and MCP server discovery.

These instructions are rendered for `{{ .AgentID }}`.

Authentication is managed by `obot login`. Do not ask the user for Obot API tokens, and do not call Obot APIs directly.

### Skill Discovery

To find installable skills from Obot, run:

```sh
obot skills search "<query>"
```

To install a skill from Obot, run:

```sh
obot skills install --destination {{ .InstallDestination }} <skill>
```

Prefer the CLI for Obot-managed skills instead of hand-editing installed skill files from Obot.

### MCP Server Discovery

To search the configured Obot server for available MCP servers, run:

```sh
obot mcp search "<query>"
```

Omit the query to list all MCP servers:

```sh
obot mcp search
```

Always display the connection or configuration URL to the user if they asked you to find a server.
If there is a connection URL, ask the user if they would like to configure it. Then explain to them how to set it up as a remote MCP server.
If there is a configuration URL, explain to the user that they need to go to that URL and configure the server in the Obot UI.
If the user wants to install a server, explain to them how to set it up as a remote MCP server using the connection URL, if there is one. If you can do it yourself using your CLI, then ask whether they would like you to do it for them.
