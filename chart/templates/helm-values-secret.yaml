apiVersion: v1
kind: Secret
metadata:
  name: {{ include "obot.fullname" . }}-helm-values
  labels:
    {{- include "obot.labels" . | nindent 4 }}
type: Opaque
stringData:
  values.yaml: |
{{- include "obot.helmValuesSnapshot" . | nindent 4 }}
